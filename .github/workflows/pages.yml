name: Deploy to GitHub Pages

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: Version to publish (e.g. 3.0.10). Defaults to the latest release tag.
        required: false
        type: string

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  deploy-to-github-pages:
    uses: Der-Floh/Der-Floh/.github/workflows/app-pages.yml@v1
    with:
      project-path: Cursor_Installer_Creator.Browser/Cursor_Installer_Creator.Browser.csproj
      version: ${{ inputs.version }}
